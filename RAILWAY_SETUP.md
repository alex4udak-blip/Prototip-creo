# 🚂 Детальная настройка Railway для BannerGen

## Обзор архитектуры

```
┌─────────────────────────────────────────────────────────────┐
│                      Railway Project                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────┐      ┌──────────────────────────────┐ │
│  │                  │      │                              │ │
│  │   PostgreSQL     │◄─────│      BannerGen Service       │ │
│  │   (Database)     │      │      (Node.js + React)       │ │
│  │                  │      │                              │ │
│  └──────────────────┘      └──────────────────────────────┘ │
│                                      │                       │
│                                      ▼                       │
│                            ┌─────────────────┐              │
│                            │     Volume      │              │
│                            │  /app/uploads   │              │
│                            └─────────────────┘              │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Шаг 1: Подготовка GitHub репозитория

### 1.1 Создание Personal Access Token

1. Зайди на GitHub → **Settings** → **Developer settings**
2. **Personal access tokens** → **Tokens (classic)**
3. **Generate new token (classic)**
4. Настройки токена:
   - **Note:** `Railway deploy`
   - **Expiration:** Выбери срок (рекомендую 90 дней)
   - **Scopes:** Отметь `repo` (полный доступ к репозиториям)
5. Нажми **Generate token**
6. **ВАЖНО:** Скопируй токен! Он покажется только один раз.

### 1.2 Push репозитория

```bash
# Перейди в папку проекта
cd Prototip-creo

# Инициализируй git (если ещё не сделано)
git init

# Добавь все файлы
git add .

# Первый коммит
git commit -m "Initial commit: BannerGen AI Banner Generator"

# Добавь remote
git remote add origin https://github.com/alex4udak-blip/Prototip-creo.git

# Push (введи username и token как пароль)
git push -u origin main
```

---

## Шаг 2: Создание проекта в Railway

### 2.1 Вход в Railway

1. Открой [railway.app](https://railway.app)
2. Войди через **GitHub**
3. Разреши доступ к репозиториям

### 2.2 Создание нового проекта

1. Нажми **New Project** в Dashboard
2. Выбери **Deploy from GitHub repo**
3. Найди и выбери `alex4udak-blip/Prototip-creo`
4. Railway создаст проект и начнёт первый деплой (он упадёт — это нормально, нужна БД)

---

## Шаг 3: Добавление PostgreSQL

### 3.1 Создание базы данных

1. В проекте нажми **+ New**
2. Выбери **Database** → **PostgreSQL**
3. PostgreSQL создастся автоматически (~1 минута)
4. Кликни на PostgreSQL сервис, чтобы увидеть данные подключения

### 3.2 Проверка подключения (опционально)

В панели PostgreSQL ты увидишь:
- **Host:** `containers-us-west-XX.railway.app`
- **Port:** `XXXXX`
- **Database:** `railway`
- **User:** `postgres`
- **Password:** `***`

---

## Шаг 4: Настройка переменных окружения

### 4.1 Переход к настройкам

1. Кликни на сервис **BannerGen** (твоё приложение)
2. Перейди на вкладку **Variables**
3. Нажми **+ New Variable** для каждой переменной

### 4.2 Обязательные переменные

| Переменная | Значение | Описание |
|------------|----------|----------|
| `DATABASE_URL` | `${{Postgres.DATABASE_URL}}` | Кликни "Reference", выбери Postgres |
| `JWT_SECRET` | `<сгенерируй>` | Секрет для JWT токенов |
| `NODE_ENV` | `production` | Режим работы |
| `PORT` | `3000` | Порт приложения |

**Как сгенерировать JWT_SECRET:**
```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

### 4.3 API Keys (важно!)

| Переменная | Где получить | Обязательно? |
|------------|--------------|--------------|
| `RUNWARE_API_KEY` | [my.runware.ai](https://my.runware.ai) → API Keys | ✅ Да |
| `ANTHROPIC_API_KEY` | [console.anthropic.com](https://console.anthropic.com) | ⚠️ Рекомендуется |
| `GOOGLE_API_KEY` | [aistudio.google.com/apikey](https://aistudio.google.com/apikey) | ⚠️ Рекомендуется |

**Примечание:**
- Без `RUNWARE_API_KEY` генерация не будет работать
- Без `ANTHROPIC_API_KEY` промпты не будут улучшаться (работает базовый режим)
- Без `GOOGLE_API_KEY` Nano Banana модели недоступны (только FLUX)

### 4.4 Итоговый список переменных

```env
DATABASE_URL=${{Postgres.DATABASE_URL}}
JWT_SECRET=your-super-secret-key-at-least-64-characters
NODE_ENV=production
PORT=3000

RUNWARE_API_KEY=rw_xxxxxxxxxxxxxxxx
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxx
GOOGLE_API_KEY=AIzaxxxxxxxxxxxxxxxx
```

---

## Шаг 5: Добавление Volume для изображений

### 5.1 Создание Volume

1. Кликни на сервис **BannerGen**
2. Перейди на вкладку **Settings**
3. Прокрути до секции **Volumes**
4. Нажми **+ New Volume**
5. Настройки:
   - **Mount Path:** `/app/uploads`
   - **Name:** `bannergen-uploads`
6. Нажми **Add Volume**

### 5.2 Зачем нужен Volume?

- Без Volume все сгенерированные картинки удалятся при редеплое
- Volume — это persistent storage, данные сохраняются
- Размер по умолчанию: 10GB (можно увеличить в настройках)

---

## Шаг 6: Настройка домена

### 6.1 Автоматический домен Railway

1. Кликни на сервис **BannerGen**
2. Вкладка **Settings** → секция **Networking**
3. Нажми **Generate Domain**
4. Получишь URL вида: `prototip-creo-production.up.railway.app`

### 6.2 Свой домен (опционально)

1. В секции **Networking** нажми **Custom Domain**
2. Введи свой домен: `bannergen.yourdomain.com`
3. Railway покажет CNAME запись для DNS
4. Добавь CNAME в настройках DNS твоего домена:
   - **Type:** CNAME
   - **Name:** `bannergen`
   - **Value:** `xxxxxxxxx.railway.app`
5. Railway автоматически выпустит SSL сертификат

---

## Шаг 7: Запуск первого деплоя

### 7.1 Проверка статуса

После настройки переменных Railway автоматически запустит новый деплой.

Мониторинг:
1. Кликни на сервис → вкладка **Deployments**
2. Кликни на активный деплой
3. Смотри логи в реальном времени

### 7.2 Ожидаемые логи при успешном запуске

```
🚀 Starting BannerGen...
⏳ Waiting for database...
✅ Database connected
📦 Running migrations...
Migration 001_init.sql applied
👥 No users found, running seed...
Created invite: http://xxx.railway.app/invite/abc123
Created invite: http://xxx.railway.app/invite/def456
...
🎉 Starting server...
🚀 Server started on port 3000
📦 Environment: production
```

### 7.3 Проверка health endpoint

Открой в браузере:
```
https://your-app.up.railway.app/api/health
```

Ожидаемый ответ:
```json
{
  "status": "healthy",
  "timestamp": "2026-01-22T...",
  "uptime": 123.456,
  "database": "connected",
  "websocket": { "totalConnections": 0, "chatRooms": 0 },
  "environment": "production",
  "apis": {
    "claude": true,
    "runware": true,
    "google": true
  }
}
```

---

## Шаг 8: Получение invite-ссылок

### 8.1 Через Railway Shell

1. Кликни на сервис → вкладка **Settings**
2. Нажми **Connect** в секции **Shell**
3. В терминале выполни:

```bash
cd backend && npm run seed
```

### 8.2 Через Railway CLI (альтернатива)

```bash
# Установка CLI
npm install -g @railway/cli

# Логин
railway login

# Привязка к проекту
railway link

# Запуск seed
railway run npm run seed -C backend
```

### 8.3 Результат

Ты увидишь 5 invite-ссылок:
```
============================================
          🎉 INVITE LINKS CREATED 🎉
============================================
1. https://prototip-creo-production.up.railway.app/invite/abc123def456
2. https://prototip-creo-production.up.railway.app/invite/ghi789jkl012
...
============================================
Share these links with your team!
Each link can only be used once.
============================================
```

---

## Шаг 9: Проверка работоспособности

### 9.1 Полный чеклист

- [ ] `/api/health` возвращает `{"status": "healthy"}`
- [ ] Invite-ссылка открывает страницу входа
- [ ] После входа видишь интерфейс чата
- [ ] WebSocket подключается (нет ошибок в консоли)
- [ ] Генерация работает (пробуем простой промпт)
- [ ] Картинки сохраняются и отображаются
- [ ] Чаты сохраняются в истории

### 9.2 Тестовый промпт

После входа попробуй:
```
Баннер казино "WELCOME BONUS 500€", золотой стиль, размер Facebook Feed
```

---

## Troubleshooting

### ❌ "Database connection failed"

**Проверь:**
1. PostgreSQL сервис запущен (зелёная точка)
2. `DATABASE_URL` указывает на `${{Postgres.DATABASE_URL}}`
3. Нет опечаток в Reference

**Решение:**
```bash
# В Railway Shell
echo $DATABASE_URL  # Должен показать postgres://...
```

### ❌ "ANTHROPIC_API_KEY not set"

Это warning, не ошибка. Система работает без Claude API, но:
- Промпты не улучшаются
- Работает базовый режим
- Названия чатов генерируются обрезкой текста

### ❌ "Картинки не сохраняются"

**Проверь:**
1. Volume примонтирован к `/app/uploads`
2. В логах нет ошибок записи
3. Права на директорию (должны быть 777)

### ❌ "WebSocket не подключается"

Railway поддерживает WebSocket из коробки. Проверь:
1. Фронтенд использует тот же домен
2. Нет CSP/CORS ошибок в консоли браузера

### ❌ "Build failed: npm ci"

**Возможные причины:**
1. Несовместимость версий в package-lock.json
2. Ошибки в package.json

**Решение:**
```bash
# Локально
rm -rf node_modules package-lock.json
npm install
git add package-lock.json
git commit -m "Fix: Regenerate package-lock"
git push
```

---

## Полезные команды Railway CLI

```bash
# Установка
npm install -g @railway/cli

# Логин
railway login

# Привязка к проекту (выполни в папке проекта)
railway link

# Запуск команды в контейнере
railway run <command>

# Просмотр логов
railway logs

# Открыть shell
railway shell

# Получить переменные окружения
railway variables

# Редеплой
railway up
```

---

## Обновление проекта

После изменений в коде:

```bash
git add .
git commit -m "Update: ваше описание"
git push origin main
```

Railway автоматически:
1. Получит изменения
2. Запустит Docker build (~2-3 минуты)
3. Применит миграции
4. Перезапустит сервер
5. Переключит traffic на новую версию

**Zero-downtime deployment** — пока новая версия билдится, старая продолжает работать.

---

## Мониторинг

### Metrics (встроенные)

В Railway Dashboard → Service → Metrics:
- CPU Usage
- Memory Usage
- Network I/O
- Request Count

### Логи

```bash
# Через CLI
railway logs --follow

# Или в Dashboard → Deployments → View Logs
```

### Алерты (Pro план)

Railway Pro позволяет настроить алерты на:
- High CPU/Memory
- Deployment failures
- Downtime

---

## Бюджет и лимиты

### Railway Free Tier

- $5/month free credits
- Достаточно для ~500 часов работы
- Автопауза при исчерпании

### Railway Developer Tier ($5/month)

- $5 included credits
- No sleep (сервис всегда online)
- Priority support

### Примерная стоимость BannerGen

При среднем использовании:
- Compute: ~$2-3/month
- PostgreSQL: ~$1/month
- Volume: ~$0.5/month
- **Итого: ~$3.5-4.5/month**

---

Ready to go! 🚀

При вопросах смотри:
- [Railway Docs](https://docs.railway.app)
- [Railway Community](https://discord.gg/railway)
